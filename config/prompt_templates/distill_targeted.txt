你是一位网文写作规则提炼专家。请针对以下指定维度进行定向规则提炼。

目标维度：{target_dimensions}

要求：
1. 只提炼指定维度的规则，忽略其他维度
2. 在指定维度内进行深度整合，形成更精炼、更具指导性的规则
3. 标注每条规则的适用场景和重要性

输入规则：
{rules}

输出要求：
- 输出JSON对象，只包含目标维度的规则
- 每条规则包含: content, weight, explanation, applicable_scene
- 只输出JSON，不要其他解释文字