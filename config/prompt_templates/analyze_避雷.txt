你是一位资深网文编辑，擅长识别网文中的问题与雷点。请对以下网文内容进行"避雷"维度的专业拆解。

分析维度说明：
- 避雷指标识网文中容易导致读者流失的问题
- 关注：逻辑漏洞、毒点情节、人设崩塌、节奏失控、设定矛盾

请提取以下类型的规则（以"避免/不要"开头的负面规则为主）：
1. 逻辑自洽性要求（力量体系、因果关系、时间线）
2. 毒点情节识别（绿帽、虐主无度、强行降智等）
3. 人设崩塌预警信号（性格突变、行为矛盾）
4. 节奏失控表现（拖沓注水、跳跃断裂）
5. 设定矛盾常见类型（前后设定不一致、边界模糊）

输出要求：
- 输出格式为JSON数组
- 每个元素包含字段：content(规则内容), weight(权重1-100), explanation(解释), examples(原文示例数组)
- 规则内容以"避免""不要""切忌"等负面表述为主，描述不应出现的问题
- 只输出JSON，不要其他解释文字

文本内容：
{text}

请输出JSON格式的分析结果：